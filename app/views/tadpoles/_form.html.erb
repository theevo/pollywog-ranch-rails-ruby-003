<%= form_for(@tadpole) do |f| %>
  <% if @tadpole.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tadpole.errors.count, "error") %> prohibited this frog from being saved:</h2>

      <ul>
      <% @tadpole.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
    <%= f.hidden_field :frog_id, :value => @tadpole.frog_id %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
